'use strict';

/* Directives */


angular.module('myApp.directives', []).
  directive('appVersion', ['myTest', function(list) {
    return function(scope, elm, attrs) {
      elm.text(list);
    };
  }]);
